[project]
name = "sloan-sim"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pytest>=9.0.3",
]

[tool.pytest.ini_options]
pythonpath = ["src"]